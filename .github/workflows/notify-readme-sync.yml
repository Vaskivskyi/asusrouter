name: Notify README sync

permissions:
  contents: read

on:
  push:
    branches:
      - dev
    paths:
      - "readme/shared/**"
      - "scripts/build_readme.py"
  workflow_dispatch:

concurrency:
  group: notify-readme-sync
  cancel-in-progress: true

jobs:
  notify-ha:
    name: Notify ha-asusrouter
    runs-on: ubuntu-latest
    steps:
      - name: Create GitHub App token
        id: app_token
        uses: actions/create-github-app-token@v3.2.0
        with:
          client-id: ${{ secrets.AR_APP_ID }}
          private-key: ${{ secrets.AR_APP_KEY }}
          repositories: ha-asusrouter

      - name: Notify ha-asusrouter
        uses: peter-evans/repository-dispatch@v4.0.1
        with:
          token: ${{ steps.app_token.outputs.token }}
          repository: Vaskivskyi/ha-asusrouter
          event-type: ar_readme_sync
          client-payload: |
            {"repo":"${{ github.repository }}","ref":"${{ github.ref }}","sha":"${{ github.sha }}"}
